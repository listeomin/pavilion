# 🦜 Hhrrr Pavilion
Минималистичный анонимный чат с рандомными именами и чистым дизайном.

[https://hhrrr.ru/pavilion/](Hhrrr.ru/Pavilion)

## Особенности
* **Анонимность** — рандомные имена с эмодзи из `user_names.json` (🐺 вульв, 🦊 лисус, 🦉 совун...)
* **Markdown форматирование** — поддержка bold (`**текст**`), italic (`*текст*`), code (`` `код` ``)
* **WYSIWYG редактор** — live preview markdown при вводе
* **Горячие клавиши** — Cmd/Ctrl+B (bold), Cmd/Ctrl+I (italic), Cmd/Ctrl+E (code), Cmd/Ctrl+Z (undo), Cmd/Ctrl+Shift+Z (redo)
* **Меню форматирования** — всплывающая панель при выделении текста (B, i, code)
* **Undo/Redo** — история изменений до 50 шагов
* **Автопревью изображений** — ссылки на .png/.jpg/.gif/.webp автоматически отображаются как картинки
* **Inline команды** — система `/music: Artist – Track` для добавления треков
* **Минималистичный дизайн** — Georgia 18px, без рамок и скроллбаров
* **Плавные анимации** — fade-in появление сообщений, градиент растворения при скролле
* **Умная форма ввода** — кнопка [отправить] появляется только при вводе текста
* **Персистентные сессии** — история сохраняется через cookie
* **Real-time обновления** — polling каждые 3 секунды

## Inline Commands System

Система команд с терминальным интерфейсом прямо в поле ввода:

### `/music:` команда
- **Навигация**: Tab/Arrow Up = автокомплит вперёд, Arrow Down = шаг назад
- **Ввод исполнителя**: `/music: J` → показывает hint "oy Division" серым
- **Автокомплит**: `/music: Joy` + Arrow Up → `/music: Joy Division – `
- **Ввод трека**: `/music: Joy Division – A` → hint "tmosphere" серым
- **Полное совпадение**: всё зелёное = кнопка [отправить] становится фиолетовой
- **Backspace защита**: при удалении с ` – ` удаляется весь блок целиком
- **Курсор**: не может находиться между `–` и пробелом

### Цветовая схема
- **Фиолетовый** (`cmd-prefix`): `/music:`
- **Серый** (`cmd-hint`): подсказки автокомплита (невыделяемые)
- **Чёрный** (`cmd-query`): введённый текст
- **Зелёный** (`cmd-suggestion`): валидные совпадения из базы

### Логика кнопки [отправить]
- **Обычный текст**: `a` → кнопка видна (серая)
- **Команда**: `/` → кнопка скрыта
- **Команда**: `/music:` → кнопка скрыта
- **Команда**: `/music: a` → кнопка видна (серая)
- **Полная команда**: `/music: Joy Division – Atmosphere` → кнопка фиолетовая
- **Кнопка обновляется**:
  - При вводе/удалении символов (включая backspace)
  - При автокомплите (Tab / Arrow Up)
  - При откате назад (Arrow Down)

### База музыки
`/public/data/music-test.json` — 10 артистов × 10 треков (Joy Division, The Cure, Radiohead, Кино, Аквариум...)

## Технологии
* **Backend:** PHP 8.5+, SQLite
* **Frontend:** Vanilla JS ES6 modules, contenteditable для WYSIWYG, Georgia + Ubuntu Mono
* **Архитектура:** Repository pattern, REST API, модульная структура

## Быстрый старт

### Локальная разработка
```bash
# Клонировать репозиторий
git clone https://github.com/listeomin/pavilion.git
cd pavilion

# Запустить dev server
php -S localhost:8000 -t public router.php

# Открыть в браузере
open http://localhost:8000
```

### Заполнение тестовыми данными
```bash
php seed_messages.php
```

### Production (nginx)
На production сервере настроить nginx для работы с `/pavilion/` путём.
Пример конфигурации:
```nginx
location /pavilion {
    alias /path/to/pavilion/public;
    try_files $uri $uri/ /pavilion/index.php?$args;
    
    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.5-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $request_filename;
        include fastcgi_params;
    }
}
```

## Структура проекта
```
pavilion/
├── .gitignore              # игнорируемые файлы (*.sqlite, .DS_Store)
├── README.md               # документация
├── router.php              # роутер для PHP dev server
├── seed_messages.php       # скрипт для заполнения тестовыми данными
├── public/                 # фронтенд (document root)
│   ├── index.php          # главная страница
│   ├── css/               # стили (модульная структура)
│   │   ├── base.css       # типография, layout, общие стили
│   │   ├── chat.css       # сообщения, анимации, градиент
│   │   ├── input.css      # форма ввода, кнопка отправить
│   │   └── format-menu.css # всплывающее меню форматирования
│   └── js/                # логика (ES6 модули)
│       ├── config.js      # конфигурация путей (localhost vs production)
│       ├── api.js         # API запросы (init, send, poll, cookies)
│       ├── markdown.js    # парсинг markdown, linkify изображений
│       ├── render.js      # рендеринг сообщений, управление UI
│       ├── editor.js      # Editor class (cursor, history, undo/redo)
│       ├── format.js      # FormatMenu class (всплывающее меню)
│       ├── hotkeys.js     # горячие клавиши (Cmd+B/I/E, Enter, Undo/Redo)
│       └── main.js        # entry point (68 строк)
└── server/                 # бэкенд
    ├── api.php            # REST API (init, send, poll)
    ├── db.php             # подключение к SQLite + миграции
    ├── SessionRepository.php   # управление сессиями
    ├── MessageRepository.php   # управление сообщениями
    └── user_names.json    # список рандомных имён с эмодзи
```

## Архитектура фронтенда

### Модульная структура
Код разбит на независимые модули с четким разделением ответственности:

**CSS модули:**
- `base.css` — базовые стили, типография, layout
- `chat.css` — стили чата, анимации сообщений
- `input.css` — стили формы ввода
- `format-menu.css` — стили всплывающего меню

**JS модули (ES6):**
- `config.js` — конфигурация окружения
- `api.js` — работа с API, управление cookies
- `markdown.js` — парсинг markdown, обработка ссылок/изображений
- `render.js` — рендеринг DOM, обновление UI
- `editor.js` — класс Editor для работы с contenteditable
- `format.js` — класс FormatMenu для всплывающего меню
- `hotkeys.js` — обработка горячих клавиш
- `main.js` — точка входа, инициализация приложения

## База данных

**sessions**
- `id` (TEXT, PRIMARY KEY) — UUID сессии
- `name` (TEXT, UNIQUE) — имя пользователя (🐺 вульв, 🦊 лисус...)
- `created_at` (TEXT) — дата создания (ISO 8601)

**messages**
- `id` (INTEGER, PRIMARY KEY, AUTOINCREMENT)
- `session_id` (TEXT, FOREIGN KEY → sessions.id)
- `author` (TEXT) — имя автора
- `text` (TEXT) — текст сообщения
- `created_at` (TEXT) — дата создания (ISO 8601)

База создаётся автоматически при первом обращении к API.

## API

### `GET/POST /server/api.php?action=init`
Инициализация сессии. Если есть cookie `chat_session_id` — возвращает полную историю, иначе создаёт новую сессию и возвращает последние 50 сообщений.

**Response:**
```json
{
  "session_id": "7e876ec522f8c672956c2b0963c60888",
  "name": "🐺 вульв",
  "is_new": false,
  "messages": [...]
}
```

### `POST /server/api.php?action=send`
Отправка сообщения.

**Request:**
```json
{
  "session_id": "7e876ec522f8c672956c2b0963c60888",
  "text": "Привет!"
}
```

**Response:**
```json
{
  "id": 42,
  "session_id": "7e876ec522f8c672956c2b0963c60888",
  "author": "🐺 вульв",
  "text": "Привет!",
  "created_at": "2025-12-10T14:39:03+00:00"
}
```

### `GET /server/api.php?action=poll&after_id=41`
Получение новых сообщений после указанного ID.

**Response:**
```json
{
  "messages": [...]
}
```
