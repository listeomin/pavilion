# Тесты для проблемы Rebase

## Проблема
После команды `/rebase` первое сообщение не отправляется с ошибкой "🛳️ капитанская рубка: у нас проблемы. [переотправить]". Требуется обновление страницы для продолжения работы.

## Причина
При выполнении `/rebase` очищается база данных, включая все сессии. Клиент продолжает использовать старый `session_id`, который больше не существует.

## Созданные тесты

### JavaScript тесты (6 файлов)

#### 1. rebase-flow.test.js
**Базовые API тесты**
- `apiRebase()` вызывает endpoint
- Возвращает success и messages
- Обрабатывает ошибки
- Старая сессия становится невалидной
- Новая сессия работает после rebase

#### 2. rebase-integration.test.js
**Полный интеграционный сценарий**
- Воспроизведение бага (старая сессия → ошибка)
- Показывает ошибку в UI
- Решение через реинициализацию
- WebSocket переподключение
- Полный цикл rebase
- Edge cases (отправка во время rebase, множественные rebases)

#### 3. websocket-rebase.test.js
**WebSocket клиент**
- Получение события `rebase`
- Обработка пустого массива messages
- Множественные listeners
- Состояние соединения после rebase
- Отправка сообщений после rebase
- session_id в URL соединения
- Переподключение с новым session_id
- Автоматический reconnect vs manual disconnect

#### 4. main-rebase.test.js
**Интеграция в main.js**
- Обработка команды `/rebase`
- Очистка чата
- WebSocket события
- Рендеринг сообщений
- Восстановление сессии
- Отправка после восстановления
- WebSocket переподключение
- UI состояние (фокус, очистка input, обновление emoji)

#### 5. rebase-ui-state.test.js
**Состояние UI компонентов**
- Очистка/сохранение input
- Фокус input
- Очистка chat log
- Спиннер во время rebase
- Сообщения об ошибках
- Рендеринг seed messages
- Состояние кнопки send
- Retry функциональность
- Отслеживание sessionId
- Обновление имени пользователя
- WebSocket connection indicator
- Message history

#### 6. REBASE_TESTS_README.md
**Документация**
- Описание проблемы
- Причина
- Решение
- Структура тестов
- Ожидаемое поведение
- Изменения в коде
- Тестовое покрытие

### PHP тесты (1 файл)

#### ApiHandlerRebaseTest.php
**Серверная логика**
- Структура ответа rebase (без реального скрипта)
- Старые сессии невалидны после rebase
- Новые сессии работают после rebase
- Broadcast в WebSocket
- Очистка сообщений
- Возврат seeded messages
- Множественные сессии инвалидируются
- Init создаёт новую сессию после rebase

## Общее покрытие

### Функциональность
- ✅ API endpoint `/rebase`
- ✅ Инвалидация старых сессий
- ✅ Создание новых сессий
- ✅ WebSocket события
- ✅ WebSocket переподключение
- ✅ UI обновления
- ✅ Обработка ошибок
- ✅ Edge cases

### Компоненты
- ✅ api.js (apiRebase, apiInit, apiSend)
- ✅ websocket-client.js (события, reconnect)
- ✅ main.js (обработчики, UI)
- ✅ ApiHandler.php (rebase метод)
- ✅ SessionRepository.php (создание сессий)

### Сценарии
- ✅ Успешный rebase
- ✅ Неудачный rebase
- ✅ Отправка с старой сессией → ошибка
- ✅ Реинициализация → успех
- ✅ WebSocket переподключение
- ✅ Множественные rebases
- ✅ Rebase во время отправки
- ✅ Сохранение пользовательского ввода

## Запуск

```bash
# Все JS тесты
npm test

# Конкретная группа
npm test rebase

# PHP тесты
./test-php.sh

# Конкретный PHP класс
./vendor/bin/phpunit tests/php/ApiHandlerRebaseTest.php
```

## Статистика

- **Всего тестов**: ~50+
- **JS файлов**: 6
- **PHP файлов**: 1
- **Тестовых случаев**: ~45+
- **Покрытие**: API, WebSocket, UI, Server

## Решение (для продакшн кода)

После реализации решения в `main.js`:

```javascript
wsClient.on('rebase', async (data) => {
  // 1. Clear UI
  chatLog.innerHTML = '';
  lastIdRef.value = 0;
  
  // 2. Re-init session
  const initData = await apiInit(API, null, COOKIE_NAME);
  sessionId = initData.session_id;
  myName = initData.name;
  
  // 3. Update UI
  userEmojiEl.textContent = myName.split(' ')[0];
  
  // 4. Reconnect WebSocket
  wsClient.disconnect();
  wsClient = new WebSocketClient(CONFIG.WS_URL, sessionId);
  wsClient.connect();
  
  // 5. Render messages
  renderMessages(chatLog, data.messages || [], lastIdRef);
});
```

Все тесты должны пройти ✅
